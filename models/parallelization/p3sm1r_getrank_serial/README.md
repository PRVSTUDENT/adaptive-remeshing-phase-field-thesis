# P3-SM1R documented GETRANK package

Preparation-only serial technical qualification based on P3-SM0. It tests
the documented `CALL GETRANK(KPROCESSNUM)` interface inside the controlled
UEL callback.

The deck and transfer include are byte-identical to P3-SM0. The only source
changes are the integer declaration and the before/call/after lines recorded
in `P3SM1R_SOURCE_DIFFERENCE_REPORT.md`. Execution remains unauthorized.
