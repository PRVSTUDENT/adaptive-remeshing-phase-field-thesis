# Raw P3-S failure evidence

Lightweight read-only copies from:

`/scratch/pr21vyci/adaptive-remeshing/runs/p3s_serial_1378028.mmaster02`

The inventory, hashes, timestamps, missing-file list, and heavy-file policy are recorded in:

`results/validation/stage_p/p3s_signal11_forensics/P3S_FAILURE_FILE_MANIFEST.json`

The 247,488-byte ODB remains in cluster scratch and is represented by metadata only. The generated `.env` is also metadata-only because it contains a PBS job cookie. No core, SIM, ODB, credential-bearing environment dump, or other heavy output is stored here. These copies do not replace or modify the previously committed P3-S status evidence.
