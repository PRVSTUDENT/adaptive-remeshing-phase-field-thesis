# Session Report: F43PRE3-R3 Robust PBS Working-Directory Contract & Requalification

**Date**: 2026-08-08  
**Agent**: gemini-antigravity  
**Task ID**: F43PRE3-R3 / F43PRE3_GEOM  
**Starting Commit**: `ef5031be2a4573df2cce3029aa033490fd02580d`  
**Preparation Commit ($P_{R3}$)**: `b98ff859539e023f808926c6578c3d57a94c72c2`  
**Qualification Commit ($Q_{R3}$)**: `6fdf2d98398f34b09c721d9256d309de127ad095`  
**Status**: `qualified_not_authorized`  

---

## Executive Summary

Pursuant to explicit user governance directives regarding failed job `1385460.mmaster02`, this session established a robust, fail-closed working-directory contract across the submission wrapper (`submit_f43pre3_geom.sh`) and PBS batch script (`F43PRE3_GEOM.pbs`), implemented a unit test suite testing scheduler spool location independence and historical failure mode prevention, executed a 530-test Linux-Git detached qualification, and updated project coordination ledgers. Zero HPC jobs were submitted.

---

## 1. Governance & Root Cause Analysis

- **Job 1385460 Evaluation**: Job `1385460.mmaster02` consumed the single authorized submission attempt (`MAX_SUBMISSIONS=1`). The solver was never invoked; failure occurred pre-solver because OpenPBS executed `F43PRE3_GEOM.pbs` from repository root `/home/pr21vyci/projects/adaptive-remeshing` rather than the package directory `models/generated/mode_ii/f43_stage_c_bridge/`.
- **Governance Resolution**: The consumed authorization cannot be reused. A new explicit human authorization in chat is strictly required before any replacement submission.

---

## 2. Robust Working-Directory Architecture Contract

1. **Submission Wrapper (`submit_f43pre3_geom.sh`)**:
   - Computes `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`.
   - Navigates: `cd "${SCRIPT_DIR}"` and asserts `[ "$(pwd)" = "${SCRIPT_DIR}" ]`.
   - Verifies local package file existence (`F43PRE3_GEOM.inp`, `F43PRE3_GEOM.pbs`, `collect_f43pre3_geom_evidence.sh`, `validate_f43pre3_geom_runtime.py`).
   - Verifies exact input deck SHA `10d4fb75cc97d92fbb1491361624e92f4cc4269ed40e4420164af28ed15207ee`.
   - Asserts `MAX_SUBMISSIONS=1`, `AUTOMATIC_RETRY=false`, `REPLACEMENT_AUTHORIZED=false` (unless authorized), and `qstat` clean before calling `qsub`.

2. **PBS Script (`F43PRE3_GEOM.pbs`)**:
   - DOES NOT rely on `BASH_SOURCE[0]` to locate the package directory (avoiding OpenPBS spool directory traps such as `/var/spool/pbs/spool/1385460.OU`).
   - Uses `PBS_O_WORKDIR` as authoritative package anchor: `: "${PBS_O_WORKDIR:?PBS_O_WORKDIR is required}"`, `WORKDIR="${PBS_O_WORKDIR}"`, `cd "${WORKDIR}" || exit 1`, `[ "$(pwd)" = "${WORKDIR}" ] || exit 1`.
   - Performs pre-solver fail-closed package checks (input deck presence, input deck SHA, source CAE SHA, module loading) before calling `abaqus`.
   - Collector Contract: calls `${WORKDIR}/collect_f43pre3_geom_evidence.sh "${PBS_JOBID:-local}"`. Preserves solver exit code (`solver_rc`); exits `final_rc` (`solver_rc` if non-zero else `collector_rc`).

---

## 3. Working-Directory Unit & Regression Test Suite

Created `tests/unit/test_f43pre3_r3_working_directory_contract.py` (7 tests):
- **Test A**: Wrapper invoked from repository root -> fake `qsub` CWD = `models/generated/mode_ii/f43_stage_c_bridge` (**PASS**).
- **Test B**: Wrapper invoked from arbitrary temporary directory -> fake `qsub` CWD = `models/generated/mode_ii/f43_stage_c_bridge` (**PASS**).
- **Test C**: PBS script pre-solver shell portion executed with valid `PBS_O_WORKDIR` -> finds input deck (**PASS**).
- **Test D**: PBS script executed from scheduler spool directory (`/tmp/fake_spool/1385460.OU`) -> resolves package via `PBS_O_WORKDIR` (**PASS**).
- **Test E**: Historical 1385460 failure mode regression test -> fails closed when `PBS_O_WORKDIR` is repo root (**PASS**).
- **Test F**: Missing `PBS_O_WORKDIR` negative contract test -> fails closed (**PASS**).
- **Test G**: Input deck SHA mismatch negative contract test -> fails closed (**PASS**).

---

## 4. Linux-Git Detached Qualification Results

Executed `scripts/validation/run_f43pre3_r3_detached_qual.sh` at exact $P_{R3}$ (`b98ff859539e023f808926c6578c3d57a94c72c2`):
- **Discovered Unit Tests**: **530 passed** (0 failures, 0 errors, 0 skips across 60 files).
- **Static Runtime Validator**: **PASS** (`validate_f43pre3_geom_runtime.py`).
- **Semantic Equivalence Validator**: **PASS** (`validate_f43pre3_semantic_equivalence.py`).
- **Working-Directory Contract Suite**: **PASS** (7/7 tests passed).
- **Detached Worktree Status Gate**: **PASS** (`F43PRE3_R3_DETACHED_QUALIFICATION_SUCCESS`, `git status --porcelain=v1` empty).

---

## 5. Verification Hashes

- `F43PRE3_GEOM.inp` SHA256: `10d4fb75cc97d92fbb1491361624e92f4cc4269ed40e4420164af28ed15207ee`
- `F43PRE3_GEOM.pbs` SHA256: `7ae4ca1b5bdca1a9eeeaecae4cbbacb8fc6be5e76a66fbbf4974fbf1d6cfb33b`
- `submit_f43pre3_geom.sh` SHA256: `3b8d4f40f0fcb0ef0bfceefb1ebfecba46002fec5caacfd1d94beaa1d784a955`
- `collect_f43pre3_geom_evidence.sh` SHA256: `a93be06ef1cbcd5022878d6b9ceee499d3752e505a41ea4aa0f00f074dcfc9a4`
- `test_f43pre3_r3_working_directory_contract.py` SHA256: `209a85cebbab5c57b545f470559f1eb1752b04bdceb693e50669aa5ec69dc521`
- `run_f43pre3_r3_detached_qual.sh` SHA256: `d9e03f0b2f3fb8f480aa29ff4cdfaae4d5aa61feee3ceb50bdf60ffbca06122d`
- `ModeII_Geometry_Source_Abaqus2023.cae` SHA256: `0d5b32fe48b70ed0817e8b9c439bfdb39165dee5e8d157fcb6d0b3075efe1baa`

---

## 6. Current Governance State

- `execution_authorized`: `false`
- `submission_approved`: `false`
- `maximum_jobs_now`: 0
- `HPC_submissions`: 0
- `qsub_called`: `false`
- `next_action`: `stopped_awaiting_explicit_human_authorization_block_for_single_replacement_F43PRE3_GEOM_submission`
