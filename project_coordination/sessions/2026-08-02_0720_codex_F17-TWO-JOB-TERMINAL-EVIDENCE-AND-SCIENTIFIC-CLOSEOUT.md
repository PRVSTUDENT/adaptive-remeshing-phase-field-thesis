# F17 terminal evidence and scientific closeout

Starting SHA `a2d5d5e`; jobs `1381483` and `1381484`. Read-only qstat confirmed
terminal exits 0 and 1; tracejob had no past-day records. The retained
lightweight inventory contains 51 artifacts before the inventory file itself.
Probe extraction passed 6/6 and penalty activation qualified. Adaptive CAE
failed on missing `F17_SOURCE_ODB` before deck import; all execution counters
are zero. Four Telegram events passed. No scheduler mutation or retry occurred.
