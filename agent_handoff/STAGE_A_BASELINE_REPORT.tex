\documentclass[11pt,a4paper]{article}

\usepackage[margin=25mm]{geometry}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{float}
\usepackage{hyperref}
\usepackage{siunitx}
\usepackage{xcolor}
\usepackage{longtable}
\usepackage{array}

\graphicspath{{../../results/figures/stage_a_baseline/}}

\title{Stage A Baseline Report\\Adaptive Remeshing Phase-Field Thesis}
\author{Pruthvi Raj Vakkalagadda}
\date{Updated 2026-07-16}

\begin{document}
\maketitle

\section*{Purpose and Maintenance Rule}

\noindent\textbf{Stage:} Stage A\\
\textbf{Document status:} active living document\\
\textbf{Last updated:} 2026-07-16

\medskip

This report is the high-level living \LaTeX{} report for Stage A: environment/toolchain verification, original Molnar source preservation, unchanged one-element verification, and unchanged notched-benchmark reproduction. It should be updated after each substantial Stage A environment, run, validation, result, figure, table, or scientific decision until Stage A is formally closed. Detailed run failures, repairs, retries, and execution evidence are recorded in the companion living log \texttt{docs/reports/STAGE\_A\_EXECUTION\_AND\_FAILURE\_LOG.tex}. These two Stage A files continue together; a new report pair is opened only when the project intentionally moves to Stage B. This report is a reproducibility and scientific-review report, not a claim that remeshing or state transfer has been validated.

\section*{Current Gate Status}

\begin{table}[H]
\centering
\caption{Project gate status at this report revision.}
\begin{tabular}{@{}ll@{}}
\toprule
Gate & Status \\
\midrule
WP0 environment/toolchain gate & Passed locally \\
HPC PBS/environment execution & Passed in final environment rerun \\
HPC GCC/Intel/Abaqus executable visibility & Passed in final environment rerun \\
HPC Abaqus/Standard license checkout & Passed in trivial user-subroutine smoke \\
HPC trivial user-subroutine compile/link & Passed \\
HPC trivial Abaqus/Standard analysis and ODB creation & Passed \\
HPC \texttt{UEXTERNALDB} callback invocation & Passed in deterministic retry \\
HPC trivial user-subroutine smoke overall & \texttt{hpc\_user\_subroutine\_smoke\_pass} \\
WP1 one-element technical gate & Passed \\
WP1 one-element scientific gate & Passed provisionally \\
WP2 single-notch technical gate & Passed \\
Paper-matched candidate-v2 HPC technical run & \texttt{paper\_matched\_v2\_technical\_pass} \\
Paper-matched candidate-v2 scientific comparison & \texttt{scientific\_review\_required} \\
MISESERI pre-refinement & Blocked until WP2 review \\
Remeshing/state transfer & Not started \\
\bottomrule
\end{tabular}
\end{table}

\section{Current HPC Technical Gate Status}

The HPC environment-only smoke sequence is now complete. The first environment smoke job, \texttt{1374529.mmaster02}, reached the compute node and confirmed Abaqus visibility, but failed when \texttt{ifx --version} returned Intel compiler error \texttt{\#10417} because the required GCC environment was not resolved on the compute node. The first rerun, \texttt{1374530.mmaster02}, loaded \texttt{gcc/11.4.0} before \texttt{intel/2024.2.0}, but failed before compiler diagnostics because \texttt{git} was unavailable in the batch environment. The final environment rerun, \texttt{1374531.mmaster02}, supplied the repository revision through \texttt{PROJECT\_REVISION} and passed with \texttt{gcc/11.4.0}, \texttt{intel/2024.2.0}, \texttt{abaqus/2023}, valid \texttt{gcc}, \texttt{gfortran}, \texttt{ifx}, and \texttt{abaqus} executables, completed compiler and Abaqus release diagnostics, and PBS \texttt{Exit\_status = 0}. The final environment classification is:

\begin{center}
\texttt{hpc\_environment\_smoke\_pass}
\end{center}

The first trivial Abaqus/Standard user-subroutine smoke job, \texttt{1374532.mmaster02}, checked out an Abaqus/Standard license, compiled, linked, completed the trivial Standard analysis, wrote an ODB, and produced a \texttt{.sta} file containing \texttt{THE ANALYSIS HAS COMPLETED SUCCESSFULLY}. The required \texttt{UEXTERNALDB} callback marker file \texttt{uexternaldb\_smoke.called} was absent, so callback invocation remains unverified and the overall job failed at the evidence check. The job is therefore classified as:

\begin{center}
\texttt{hpc\_user\_subroutine\_smoke\_fail}
\end{center}

with failure category:

\begin{center}
\texttt{callback\_invocation}
\end{center}

This is not a compiler, linker, Abaqus/Standard license, solver-completion, or ODB-creation failure. It is also not a Molnar, Gate A3, MISESERI, remeshing, or state-transfer result. The component classifications preserved for this checkpoint are:

\begin{itemize}
  \item \texttt{hpc\_abaqus\_license\_checkout\_pass}
  \item \texttt{hpc\_user\_subroutine\_compilation\_pass}
  \item \texttt{hpc\_user\_subroutine\_link\_pass}
  \item \texttt{hpc\_standard\_analysis\_pass}
  \item \texttt{hpc\_odb\_creation\_pass}
  \item \texttt{hpc\_uexternaldb\_callback\_unverified}
  \item \texttt{hpc\_user\_subroutine\_smoke\_fail}
\end{itemize}

The follow-up read-only evidence investigation found no marker file in the accessible run directory, Abaqus scratch directory, stage directory, repository evidence directory, or PBS submission root. It also found no retained shared library, object file, or object module, so the linked \texttt{UEXTERNALDB} symbol, embedded marker strings, and explicit Abaqus user-library loading remain to be checked in a future instrumented retry. Current precise status is: compiler environment passed; Abaqus linking passed; Abaqus analysis passed; callback symbol presence to be checked; callback library loading to be checked; callback entry unverified; marker write location unknown. No scientific or phase-field conclusion follows from this test.

The deterministic dual-evidence retry, job \texttt{1374533.mmaster02}, was then submitted as the approved single one-CPU \texttt{testq} retry at repository revision \texttt{c5db808b4c8d9e9bd01a9e5da0bd91b173787b8e}. It used the separate source file \texttt{uexternaldb\_smoke\_getoutdir.for} and separate PBS script \texttt{abaqus\_user\_subroutine\_smoke\_retry.pbs}, leaving the failed source and PBS script from job \texttt{1374532.mmaster02} preserved as the failed-attempt baseline. The retry passed with PBS \texttt{Exit\_status = 0} on \texttt{mnode097}, Abaqus return code zero, ODB/\texttt{.sta}/\texttt{.msg} present, \texttt{THE ANALYSIS HAS COMPLETED SUCCESSFULLY} in the \texttt{.sta} file, \texttt{UEXTERNALDB\_CALLBACK\_ENTERED}, \texttt{UEXTERNALDB\_MARKER\_WRITTEN}, and \texttt{UEXTERNALDB\_ANALYSIS\_END} in the \texttt{.msg} file, and an absolute marker at \path{/scratch/pr21vyci/adaptive-remeshing/runs/abaqus_user_subroutine_smoke_retry_1374533.mmaster02/uexternaldb_smoke.called} containing \texttt{UEXTERNALDB\_SMOKE\_CALLED} with \texttt{LOP=0}. The retry is therefore classified as:

\begin{center}
\texttt{hpc\_user\_subroutine\_smoke\_pass}
\end{center}

This pass completes the trivial HPC technical smoke gate for PBS execution, the GCC/Intel/Abaqus environment, Abaqus/Standard license checkout, user-subroutine compilation and linking, trivial Standard analysis completion, ODB creation, and direct \texttt{UEXTERNALDB} callback evidence. It is still not a Molnar run, not a phase-field formulation validation, not a Gate A3 scientific comparison, and not a MISESERI, remeshing, state-transfer, production, multi-CPU, or parameter-study result.

The detailed chronological record and evidence paths are maintained in \texttt{docs/reports/STAGE\_A\_EXECUTION\_AND\_FAILURE\_LOG.tex}.

\section{Project Phase Checklist}

Detailed task, phase, and gate status is maintained in the living project checklist \texttt{docs/project/PROJECT\_PHASE\_CHECKLIST.md}.

\begin{table}[H]
\centering
\caption{Current phase progress summary; see the project checklist for evidence paths and item-level status.}
\begin{tabular}{p{0.18\linewidth} p{0.39\linewidth} p{0.25\linewidth}}
\toprule
Phase & Description & Current status \\
\midrule
WP0 & Environment, starter pipeline, and source preservation & completed \\
WP1 & One-element verification & provisional scientific pass \\
WP2A & Supplementary Molnar single-notch technical benchmark & provisional supporting evidence \\
WP2B & Paper-matched Molnar reconstruction & technical pass; scientific review required \\
Gate A3 & Uniform reference scientific justification & open: tolerances and uniform-reference justification pending \\
WP3--WP6 & MISESERI, refined benchmark, state transfer, ABAQUSER & not started or blocked downstream \\
WP7 & Reports, checklist, and thesis writing & in progress \\
\bottomrule
\end{tabular}
\end{table}

\section{WP0 Environment and Toolchain}

The local environment inspection recorded Abaqus 2024, Abaqus Python 3.10.5, Windows 11, 12 logical processors, approximately \SI{23.35}{GiB} physical memory, GNU Fortran availability, and the absence of a usable Intel Fortran toolchain on a plain shell path. The successful user-subroutine smoke gate then established the required clean-shell sequence: load Visual Studio 2022 Build Tools with \texttt{vcvars64.bat}, then Intel oneAPI with \texttt{setvars.bat intel64}. In that environment, Abaqus used Intel \texttt{ifx} 2026.0.0 for compilation and Microsoft \texttt{LINK} 14.44.35226.0 for linking.

This is a local observed toolchain pass only. It is not an official Abaqus 2024--oneAPI 2026 support claim and it does not validate any phase-field formulation.

\section{Preserved Molnar Baseline}

The Molnar and Gravouil 2017 supplementary package is preserved under \texttt{models/baseline\_original/molnar\_gravouil\_2017/}. The unchanged run copies are made from that baseline and hash-checked before execution.

\begin{table}[H]
\centering
\caption{Preserved source/deck checksums used in completed gates.}
\begin{tabular}{@{}lll@{}}
\toprule
Gate & File & SHA-256 \\
\midrule
One element & \texttt{OneElement.for} & \texttt{8688a21a987a0348f4211cb90e365a60902dce922294a915a48644c8dae067e2} \\
One element & \texttt{OneElement.inp} & \texttt{9b63615bbe335872de197b96ca0253769f4a237c1e2e384294a1d204c462f220} \\
Single notch & \texttt{SingleNotch.for} & \texttt{18944e5bb2a3b7973fd0d4bff03f8e078eef667965343d8a29156d093f53f5f1} \\
Single notch & \texttt{SingleNotch.inp} & \texttt{89ce3f32e396b0e484be6753a272dd6bbb2a2f9daff426d6a57419f57d665b72} \\
\bottomrule
\end{tabular}
\end{table}

\section{WP1 One-Element Verification}

The unchanged one-element example passed the technical gate and the source-defined scientific check under provisional numerical tolerances. The validator opens the unchanged ODB read-only, checks all \texttt{SDV1}--\texttt{SDV16}, processes every frame and integration point, and writes CSV, JSON, and Markdown evidence.

The one-element verification established:
\begin{itemize}
  \item plane-strain undamaged elasticity;
  \item degraded stress using the phase field applied by the displacement element;
  \item homogeneous phase-field relation;
  \item monotonic history field;
  \item no phase-field healing during unloading;
  \item consistency across the four integration points.
\end{itemize}

The maximum phase-relation absolute error was approximately \(5.98\times10^{-7}\). The undamaged-stress absolute errors remained below \(1.90\times10^{-6}\). The largest \texttt{SDV14}-\texttt{SDV15} difference was approximately \(4.88\times10^{-3}\), interpreted as a staggered-update difference between the phase field used by the displacement solve and the phase-element value, not as automatic source corruption.

\section{WP2 Unchanged Single-Notch Technical Benchmark}

The unchanged Molnar single-notch tension benchmark ran from
\texttt{runs/molnar\_single\_notch\_unchanged/20260714\_technical\_gate\_local/}. The run classification is \texttt{technical\_pass\_scientific\_unchecked}.

\begin{table}[H]
\centering
\caption{Single-notch technical gate checks.}
\begin{tabular}{@{}ll@{}}
\toprule
Check & Status \\
\midrule
Original source/deck preserved and hash-matched & Pass \\
Compile and link & Pass \\
Input processing & Pass \\
Abaqus/Standard completion & Pass \\
\texttt{.sta} completion status & Pass \\
ODB readability & Pass \\
Automated RF--U/SDV extraction & Pass \\
Reference RF--U comparison & Pending \\
Crack-path comparison & Pending \\
Energy-response validation & Pending \\
\bottomrule
\end{tabular}
\end{table}

The model contains 3,998 nodes and 11,790 layered elements. The extraction produced 72 RF--U/phase states. Field outputs in both steps include \texttt{RF}, \texttt{U}, and \texttt{SDV1}--\texttt{SDV16}. The ODB is about \SI{88}{MB} and is kept local rather than mirrored into Git or handoff.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\linewidth]{single_notch_rf_u_curve.pdf}
\caption{Extracted reaction force versus applied displacement for the unchanged Molnar single-notch benchmark. The curve is not yet compared to the Molnar reference curve.}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\linewidth]{single_notch_phase_history_vs_u.pdf}
\caption{Maximum \texttt{SDV14}, \texttt{SDV15}, and \texttt{SDV16} versus applied displacement. \texttt{SDV16} is the local history field \(H\), not global fracture energy.}
\end{figure}

\begin{table}[H]
\centering
\caption{Matched displacement states extracted from the unchanged benchmark.}
\begin{tabular}{@{}rrrr@{}}
\toprule
\(U_2\) & \(RF_2\) & \(\max(\mathrm{SDV15})\) & \(\max(\mathrm{SDV16})\) \\
\midrule
0.002 & 0.266812 & 0.03598 & 0.02080 \\
0.005 & 0.626645 & 0.27554 & 0.21283 \\
0.006 & 0.721406 & 0.53678 & 0.64532 \\
0.007 & 0.001395 & 1.01049 & 337.78018 \\
\bottomrule
\end{tabular}
\end{table}

The matched states show increasing damage followed by near-complete loss of load-carrying capacity. This qualitative sequence is compatible with failure, but the shape, peak, post-peak drop, and crack evolution still require quantitative comparison with Molnar and Gravouil's reference behavior before any scientific pass is claimed.

\section{Gate A3 Scientific-Comparison Status}

The first Gate A3 validator is now available at \texttt{scripts/validation/check\_molnar\_single\_notch.py}. It reads the extracted RF--U data, matched-state contour CSVs, derived reference files, tolerance configuration, and the ODB. Its current classification is:

\begin{center}
\texttt{reference\_data\_insufficient}
\end{center}

This is not a scientific mismatch. It means the simulation-side diagnostics for the smaller supplementary deck are available, but that deck should not be forced into an exact quantitative comparison with the published Fig.~7 curve. The paper Fig.~7 curve uses a larger single-notch model of about 22,000 elements, while the supplied supplementary file used here is explicitly described by the paper as a smaller tensile example of about 4,000 elements. The thesis proposal already defines the next scientific route: prepare a paper-matched Molnar Mode-I single-notch benchmark, digitize and document the applicable published curve as an approximate paper reference for that reconstructed configuration, and compare the reconstructed uniform-mesh benchmark against the paper reference. The smaller supplementary result remains supporting technical reproducibility evidence.

\begin{table}[H]
\centering
\caption{Reference-applicability matrix explaining why the smaller supplementary deck is supporting evidence rather than the exact Fig.~7 target.}
\begin{tabular}{@{}p{0.22\linewidth}p{0.28\linewidth}p{0.28\linewidth}p{0.15\linewidth}@{}}
\toprule
Criterion & Paper Fig. 6/Fig. 7 case & Supplementary deck & Applicability \\
\midrule
Geometry & Single edge notched tensile specimen & Single edge notched square plate with horizontal ligament & Close qualitative match \\
Material & \(E=210\), \(\nu=0.3\) & \(E=210\), \(\nu=0.3\) & Match \\
\(G_c\) & \(2.7\times10^{-3}\) & 0.0027 & Match \\
Length scale & Text states \(l_c=0.0075\) mm for benchmark & Deck uses \(l_c=0.015\) & Mismatch or requires selected-curve interpretation \\
Mesh and \(h/l_c\) & Fig. 7 curve label must be confirmed & \(h=0.005\), \(h/l_c=0.333\) & Requires confirmation \\
Element count & about 22,000 elements & about 4,000 physical elements; 11,790 layered elements & Mismatch for exact reference \\
Loading & bottom fixed, top moved in tension & top tied to RP \(U_2\), bottom constrained & Close match \\
Load increments & paper says tensile step reduced to \(\Delta u=10^{-5}\) mm & frozen run reaches \(U_2\approx0.007\) in two steps & Requires interpretation \\
Fig. 7 curve label & multiple \(l_c\) curves and Miehe symbols & deck has \(l_c=0.015\), smaller mesh & Not identified \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\linewidth]{single_notch_rf_u_reference_status.pdf}
\caption{RF--U comparison status. The unchanged supplementary result is plotted as supporting reproducibility evidence; the proposal-directed scientific comparison now requires a paper-matched reconstructed benchmark and a documented approximate paper reference.}
\end{figure}

\begin{table}[H]
\centering
\caption{Simulation-side RF--U metrics from the Gate A3 validator.}
\begin{tabular}{@{}lr@{}}
\toprule
Metric & Value \\
\midrule
Initial tangent stiffness, \(0\le U_2\le0.001\) & 134.61837 \\
Peak reaction force & 0.72760785 \\
Displacement at peak & 0.00610000 \\
Area under RF--U curve, \(W_{\mathrm{ext}}\) & \(2.5813743\times10^{-3}\) \\
Post-peak force drop & 0.72621262 \\
Final residual force & 0.00139523 \\
Final displacement & 0.00700000 \\
\bottomrule
\end{tabular}
\end{table}

\section{Phase Contours and Bound Diagnostic}

Figures~\ref{fig:phase-raw} and~\ref{fig:phase-clipped} show the raw and display-clipped \texttt{SDV15} contours at the matched displacement states. The clipped plots are for visualization only. Quantitative calculations must use raw values.

\begin{figure}[H]
\centering
\includegraphics[width=0.48\linewidth]{matched_state_01_Step-1_frame_0020_raw_sdv15.pdf}
\includegraphics[width=0.48\linewidth]{matched_state_02_Step-1_frame_0050_raw_sdv15.pdf}\\
\includegraphics[width=0.48\linewidth]{matched_state_03_Step-2_frame_0010_raw_sdv15.pdf}
\includegraphics[width=0.48\linewidth]{matched_state_04_Step-2_frame_0020_raw_sdv15.pdf}
\caption{Raw \texttt{SDV15} phase-field contours at \(U_2=0.002,0.005,0.006,0.007\).}
\label{fig:phase-raw}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.48\linewidth]{matched_state_01_Step-1_frame_0020_clipped_sdv15.pdf}
\includegraphics[width=0.48\linewidth]{matched_state_02_Step-1_frame_0050_clipped_sdv15.pdf}\\
\includegraphics[width=0.48\linewidth]{matched_state_03_Step-2_frame_0010_clipped_sdv15.pdf}
\includegraphics[width=0.48\linewidth]{matched_state_04_Step-2_frame_0020_clipped_sdv15.pdf}
\caption{Display-only \texttt{SDV15} contours clipped to \(0\le d\le1\). These images are for interpretation and must not replace raw values in calculations.}
\label{fig:phase-clipped}
\end{figure}

The final raw value \(\max(\mathrm{SDV15})=1.010493\) is approximately 1.05\% above the nominal phase-field upper bound. The original element calculation does not include an explicit clipping operation, so a small discrete overshoot is possible. This does not invalidate the technical gate, but it is now a required scientific diagnostic:
\begin{itemize}
  \item preserve the raw value;
  \item determine its element, integration point, and first occurrence;
  \item report both raw and display-only clipped contours;
  \item check whether \texttt{SDV14}, \texttt{SDV15}, or both overshoot;
  \item verify whether overshoot is limited to the final unstable propagation stage;
  \item do not silently clip values in quantitative calculations.
\end{itemize}

\section{Gate A3 Crack-Path Diagnostic}

The current crack-path diagnostic uses a provisional threshold \(d_{\mathrm{crit}}=0.95\), applied to raw \texttt{SDV15}. The committed crack-path reference is a qualitative horizontal ligament \(y=0\), derived from the tensile pattern in Molnar Fig.~6 and the specimen geometry rather than from pixel-digitized paper coordinates.

\begin{figure}[H]
\centering
\includegraphics[width=0.9\linewidth]{single_notch_crack_path_diagnostic_u0007.pdf}
\caption{Thresholded crack-path diagnostic at \(U_2=0.007\). Red points are elements with \(\mathrm{SDV15}\ge0.95\); the dashed line is the qualitative horizontal reference path.}
\end{figure}

\begin{table}[H]
\centering
\caption{Crack-path metrics from matched states.}
\begin{tabular}{@{}rrrrr@{}}
\toprule
\(U_2\) & Damaged elements & Connected & Extension & Max vertical deviation \\
\midrule
0.002 & 0 & false & 0.00000 & -- \\
0.005 & 0 & false & 0.00000 & -- \\
0.006 & 0 & false & 0.00000 & -- \\
0.007 & 136 & true & 0.49713 & 0.01006 \\
\bottomrule
\end{tabular}
\end{table}

At \(U_2=0.007\), the thresholded damaged region is connected, extends almost through the ligament, and remains approximately horizontal by the provisional criterion. Earlier matched states do not exceed \(d_{\mathrm{crit}}=0.95\), so they do not yet define a thresholded crack path.

\section{Bounds, Irreversibility, and Energy Diagnostics}

\begin{table}[H]
\centering
\caption{Global bound diagnostics over all available ODB frames and integration points.}
\begin{tabular}{@{}lrrrr@{}}
\toprule
Quantity & Minimum & Maximum & Below 0 count & Above 1 count \\
\midrule
\texttt{SDV14} & 0.000000 & 1.010492 & 0 & 57 \\
\texttt{SDV15} & 0.000000 & 1.010493 & 0 & 57 \\
\texttt{SDV16} & 0.000000 & 337.780182 & -- & -- \\
\bottomrule
\end{tabular}
\end{table}

The first \texttt{SDV15} overshoot occurs in Step-2, frame 13, at \(U_2=0.006300\), element 8196, integration point 2, with value 1.0057975. \texttt{SDV14} also overshoots at the same frame and integration point. The maximum absolute staggered difference \(|\mathrm{SDV14}-\mathrm{SDV15}|\) is 0.0374441 at Step-2, frame 15, \(U_2=0.006500\), element 8188, integration point 3.

The maximum \texttt{SDV15} overshoot occurs at the final frame, Step-2 frame 20, \(U_2=0.007000\), element 8196, integration point 2, with value 1.010493 and overshoot 0.010493. Overshoot affects 11 unique integration points for both \texttt{SDV14} and \texttt{SDV15}. For \texttt{SDV15}, the overshoot duration spans global frames 64--71, corresponding to \(U_2=0.006300\) through \(U_2=0.007000\); all 11 affected points remain above one in the final frame. This supports the current interpretation that the overshoot belongs to the final unstable propagation stage, not to the elastic or stable-damage response.

\texttt{SDV16} never decreases at persistent material points in the ODB scan. Raw \texttt{SDV15} has 583 small decreases above the declared numerical tolerance \(10^{-8}\), with worst drop \(2.27809\times10^{-4}\). These decreases must be interpreted in the context of staggered element updates and UMAT visualization storage before being labelled as genuine healing.

\begin{table}[H]
\centering
\caption{\texttt{SDV15} decrease categorization over consecutive converged frames.}
\begin{tabular}{@{}lr@{}}
\toprule
Category & Count \\
\midrule
Same element/integration point in consecutive frames & 583 \\
Near step transition & 0 \\
Smaller than ODB precision tolerance \(10^{-6}\) & 364 \\
Staggered synchronization candidate & 13 \\
Remaining genuine-healing candidates requiring review & 217 \\
\bottomrule
\end{tabular}
\end{table}

The largest \texttt{SDV15} decrease is at element 8300, integration point 1, between Step-2 frame 15 and Step-2 frame 16. It drops from 0.9881207 at \(U_2=0.006500\) to 0.9878929 at \(U_2=0.006600\). It is not at a step transition, is larger than \(10^{-6}\), and is not explained by the simple local \(|\mathrm{SDV14}-\mathrm{SDV15}|\) synchronization screen. This location should be inspected before declaring a true irreversibility violation.

The external work calculated by trapezoidal integration of the extracted RF--U curve is:
\[
W_{\mathrm{ext}} = 2.5813743\times10^{-3}.
\]

The current ODB/extraction does not yet provide a validated global integration contract for degraded elastic energy from \texttt{SDV12}, undamaged elastic energy from \texttt{SDV13}, or the crack-surface functional. Those quantities require integration weights and element volume/area times thickness, and the crack-surface functional also requires a valid \(\nabla d\) reconstruction or controlled output-enabled run. \texttt{SDV16} remains the local history field \(H\), not global fracture energy.

\section{Reference and Tolerance Status}

The derived reference provenance is stored in \texttt{references/derived/molnar\_gravouil\_2017/single\_notch/REFERENCE\_PROVENANCE.md}. The current \texttt{rf\_u\_reference.csv} intentionally contains no numeric coordinates because the smaller supplementary example is not the exact Fig.~7 target. \texttt{crack\_path\_reference.csv} contains only the qualitative horizontal tensile ligament reference. The next reference artifact should document the digitized published curve for the paper-matched reconstructed benchmark, including figure and curve label, axes, units, digitization method, line/symbol uncertainty, published model parameters, and matched displacement states.

The tolerance configuration is \texttt{configs/validation/stage\_a\_single\_notch\_tolerances.json}. Supervisor-approved tolerances remain pending, and provisional tolerances remain null until reference digitization uncertainty is known. Therefore this report must not use an unconditional \texttt{scientific\_pass} classification for Gate A3.

The active proposal-directed progression is:
\begin{enumerate}
  \item keep the smaller supplementary source/deck preserved unchanged as passed technical reproducibility evidence;
  \item prepare a new paper-matched Molnar Mode-I single-notch input deck using the unchanged Molnar user subroutine;
  \item match published geometry/notch, material parameters, \(G_c\), length scale \(l\), boundary conditions, prescribed displacement history, mesh resolution and \(h/l\), element type, load increments, reaction-force extraction set, phase-field output, and crack-contour displacement states as closely as the paper permits;
  \item digitize and document the applicable published Molnar curve as an approximate paper reference for the reconstructed paper-matched model;
  \item statically verify the deck and parameters before any new run;
  \item after explicit approval, run one serial HPC baseline and compare RF--U and crack contours at matched states;
  \item perform mesh, length-scale, and load-increment convergence studies to justify the uniformly fine reference before beginning Pandey--Kumar MISESERI pre-refinement.
\end{enumerate}

Decision note \texttt{docs/decisions/0002-gate-a3-reference-route.md} is retained as history but is now superseded by the proposal-directed paper-matched benchmark route. Supervisor input remains required for final tolerances or deliberate scope deviations, not for choosing whether to proceed with the paper-matched Molnar benchmark.

\section{Paper-Matched Reconstruction Audit}

A no-run source audit has now been started for the proposal-directed paper-matched Molnar single-edge-notched Mode-I benchmark. The audit files are stored under
\path{references/derived/molnar_gravouil_2017/paper_matched_single_notch/}, with a non-runnable configuration skeleton at
\path{configs/molnar_paper_matched_single_notch.yaml} and deck-construction requirements at
\path{docs/methods/MOLNAR_PAPER_MATCHED_DECK_REQUIREMENTS.md}.

\begin{table}[H]
\centering
\caption{Paper-to-model reconstruction audit summary.}
\begin{tabular}{p{0.30\linewidth} p{0.26\linewidth} p{0.34\linewidth}}
\toprule
Item & Current status & Evidence or blocker \\
\midrule
Benchmark route & Confirmed & Thesis plan WP2 starts with the single-edge-notched Mode-I benchmark before MISESERI pre-refinement. \\
Geometry and notch & Confirmed & Molnar Fig.~6a gives a 1 mm by 1 mm specimen with a 0.5 mm left-edge notch at mid-height. \\
Loading direction & Confirmed & Pure tension uses \(\alpha=90^\circ\), with bottom fixed and top moved. \\
Material and fracture parameters & Confirmed & \(E=210\ \mathrm{kN/mm^2}\), \(\nu=0.3\), \(G_c=2.7\times10^{-3}\ \mathrm{kN/mm}\), and first target \(l_c=0.0075\ \mathrm{mm}\). \\
Mesh target & Partly unresolved & Text gives about 22,000 elements and \(h=0.001\ \mathrm{mm}\) near the crack path; refined-zone dimensions, transition rule, and exact count remain unresolved. \\
Loading sequence & Partly unresolved & Text gives \(\Delta u=10^{-4}\ \mathrm{mm}\) for 500 steps, then \(\Delta u=10^{-5}\ \mathrm{mm}\); final displacement and number of fine steps remain unresolved. \\
Fig.~7 reference & Candidate identified, not digitized & The Molnar \(l_c=0.0075\ \mathrm{mm}\) curve is visible, but no approximate paper-coordinate file or uncertainty metadata exists yet. \\
Run readiness & Not runnable & Critical configuration fields remain null; no paper-matched deck has been generated. \\
\bottomrule
\end{tabular}
\end{table}

The audit preserves the key distinction: the smaller supplementary deck remains \texttt{supporting\_technical\_reproducibility\_baseline}, while the future reconstruction is only a \texttt{paper\_matched\_scientific\_benchmark\_candidate}. Gate A3 therefore remains \texttt{reference\_data\_insufficient}.

\section{Paper-Matched Candidate v1 Static Reconstruction}

On 2026-07-16 the Molnar Fig.~7 red dashed \(l_c=0.0075\ \mathrm{mm}\) curve was digitized from the local PDF page render. The raw file \path{references/derived/molnar_gravouil_2017/paper_matched_single_notch/fig7_lc_0p0075_raw.csv} preserves 877 red-threshold pixel-derived points. The processed file \path{references/derived/molnar_gravouil_2017/paper_matched_single_notch/fig7_lc_0p0075_processed.csv} contains 91 median-bin points without smoothing. The metadata file records the page/figure, axis calibration, overlap regions, peak/post-peak handling, and estimated uncertainty of approximately \(\pm 4\times10^{-5}\ \mathrm{mm}\) and \(\pm 0.015\ \mathrm{kN}\). The digitized curve is classified as \texttt{approximate\_published\_reference}, not exact author data.

The candidate loading endpoint is measured from the visible Fig.~7 extent as \(u_\mathrm{final}=0.0067\ \mathrm{mm}\). Together with the text value \(\Delta u=10^{-4}\ \mathrm{mm}\) for 500 increments, this gives \(u=0.005\ \mathrm{mm}\) after the coarse stage and a derived 170 fine increments of \(\Delta u=10^{-5}\ \mathrm{mm}\). The exact equality of final plotted and final analysis displacement is inferred only for candidate v1.

The mesh recipe is documented as an \texttt{adopted\_reconstruction\_choice}: local \(h=0.001\ \mathrm{mm}\), \(h/l=0.133333\), refined strip \(x=-0.02..0.5\ \mathrm{mm}\), \(y=-0.005..0.005\ \mathrm{mm}\), global size 0.025 mm, linear size growth, maximum neighbouring-size ratio 1.5, full-domain quadrilateral skeleton, and U1/U2/CPS4 layer offsets by \(N_\mathrm{elem}\). The estimator and generator report 21,760 physical elements and 65,280 layered elements, which is within about 1.1\% of the paper's approximate 22,000-element statement. This agreement is a consistency check only; the mesh was not tuned to force the exact count.

The initial static validation produced \texttt{reconstruction\_inputs\_incomplete}. The later provenance review below supersedes this with \texttt{static\_validation\_fail}. No simulation result exists for this candidate, \texttt{runnable} remains \texttt{false}, and no Abaqus job has been submitted.

\section{Candidate v1 Provenance Review}

The no-run provenance review is stored at \path{results/validation/molnar_paper_matched_single_notch_v1/PARAMETER_PROVENANCE_REVIEW.md}. It compared the configuration, generated parameter snapshot, generation manifest, mesh statistics, digitized Fig.~7 data, and generated deck.

The review accepted the explicitly labelled reconstruction choices as documented choices, including the refined-zone bounds, global mesh size, transition rule, maximum size ratio, final displacement measured from Fig.~7, derived fine increment count, and response-based contour states. These choices remain non-published assumptions and do not close Gate A3.

The generated deck failed static validation for structural and loading reasons: no explicit notch representation was found, no UEL property blocks were written for the U1/U2 layers, no in-plane rigid-body constraint equivalent to the supplementary deck was found, and Step~1 implements \(0.005/500=10^{-5}\ \mathrm{mm}\) increments rather than the configured \(10^{-4}\ \mathrm{mm}\). The current classification is therefore \texttt{static\_validation\_fail}; \texttt{runnable} remains \texttt{false}. No candidate simulation result exists.

\section{Paper-Matched Candidate v2 Static Repair}

Candidate v1 is preserved as failed static evidence and was not repaired in place. Candidate v2 resolves the five static blockers in a new versioned generated directory, \path{models/generated/molnar_gravouil_2017/paper_matched_single_notch_v2/}. The loading schedule is corrected by rejecting the inconsistent \(500\times10^{-4}\ \mathrm{mm}=0.005\ \mathrm{mm}\) interpretation. Candidate v2 uses \(500\) increments of \(10^{-5}\ \mathrm{mm}\) to reach \(0.005\ \mathrm{mm}\), then \(170\) increments of \(10^{-5}\ \mathrm{mm}\) to reach the Fig.~7 endpoint \(0.0067\ \mathrm{mm}\).

The notch is represented by duplicated split nodes on the open segment \(x=-0.5..0\), \(y=0\), with separate \texttt{notch\_lower\_face} and \texttt{notch\_upper\_face} sets and no physical element bridging the open notch. The layered architecture follows the preserved supplementary implementation: U1 phase elements with \((l_c,G_c,t)\), U2 displacement elements with \((E,\nu,t,k)\), and CPS4 visualization elements with matching connectivity and offset element labels. The boundary mapping restores the supplementary \texttt{bottoml}/\texttt{topl} in-plane constraints while keeping the bottom vertical support, top-to-RP equation, and RP RF/U extraction.

The generated mesh now physically contains a refined strip and graded transition rather than only documenting them in YAML. The generated diagnostics report \(33{,}852\) physical elements, \(101{,}556\) layered elements, \(h/l=0.133333\), minimum edge size \(0.001\ \mathrm{mm}\), maximum edge size \(0.025\ \mathrm{mm}\), and maximum neighbouring-size ratio \(1.5\). The element count is higher than the approximate paper count; this is accepted as a deterministic reconstruction consequence, not a tuned match.

The executable source pairing was corrected before submission preparation. The preserved Molnar \path{SingleNotch.for} remains unchanged, while the generated candidate source \path{models/generated/molnar_gravouil_2017/paper_matched_single_notch_v2/SingleNotch_v2.for} is copied from the preserved implementation with only the hard-coded \texttt{N\_ELEM} constant updated to \(33{,}852\), matching the candidate-v2 physical element count.

Static validation passed in \path{results/validation/molnar_paper_matched_single_notch_v2/STATIC_VALIDATION.md}, with machine-readable results in \path{results/validation/molnar_paper_matched_single_notch_v2/VALIDATION_RESULTS.json}. The current configuration status is:

\begin{verbatim}
paper_matched_candidate_v2
static_validation_pass
runnable: true
\end{verbatim}

This is not a scientific validation result. No candidate-v2 Abaqus run or PBS job has been performed, and Gate A3 remains open until the paper-matched candidate is run and compared against the approximate Fig.~7 reference.

\section{Candidate v2 Mesh-Quality Preflight}

Before commit and submission preparation, a final mesh-quality preflight was written to \path{results/validation/molnar_paper_matched_single_notch_v2/MESH_QUALITY_PREFLIGHT.md}. The maximum aspect ratio remains \(25.000000000001368\), but the high-aspect-ratio elements are outside the refined fracture-process strip and do not intersect the expected horizontal crack path. The maximum-aspect element is element 9610, with bounds \(x=0.281..0.282\), \(y=-0.0792578132..-0.0542578132\), minimum distance \(0.28619\ \mathrm{mm}\) from the notch tip, positive signed area \(2.5\times10^{-5}\), and no crack-path intersection. No element bridges the open notch and the maximum neighbouring-size ratio remains \(1.5\). The aspect-ratio issue is therefore a documented reconstruction limitation rather than a submission blocker.

\section{Candidate v2 HPC Technical and Scientific Review Result}

The paper-matched candidate-v2 serial HPC baseline was submitted exactly once as job \texttt{1374864.mmaster02} from revision \texttt{711dd495bdcb830d695f9d7e56283316c9d417d5}. The job finished on \texttt{mnode099/0} with PBS \texttt{Exit\_status = 0}, walltime \texttt{00:38:38}, CPU time \texttt{00:35:52}, and run count 1. Abaqus returned zero, the ODB, \texttt{.sta}, \texttt{.msg}, and \texttt{.dat} files exist on scratch, and the \texttt{.sta} file contains \texttt{THE ANALYSIS HAS COMPLETED SUCCESSFULLY}. The technical classification is:

\begin{center}
\texttt{paper\_matched\_v2\_technical\_pass}
\end{center}

The ODB remains on scratch at \path{/scratch/pr21vyci/adaptive-remeshing/runs/molnar_paper_matched_single_notch_v2_1374864.mmaster02/molnar_paper_matched_single_notch_v2.odb}. Only lightweight scheduler, text, CSV, JSON, and Markdown evidence is retained under \path{runs/hpc/paper_matched_single_notch_v2/}.

The completed ODB was postprocessed read-only. The extracted peak reaction force is \(0.761702\ \mathrm{kN}\) at \(U_2=0.006110\ \mathrm{mm}\). The final extracted reaction force is \(0.749110\ \mathrm{kN}\) at \(U_2=0.006700\ \mathrm{mm}\). Against the approximate digitized Molnar Fig.~7 curve for \(l_c=0.0075\ \mathrm{mm}\), the original scientific check reports RF--U curve NRMSE \(0.247493\), relative peak-force error \(0.064519\), and relative peak-displacement error \(0.041257\). A no-solution forensic overlap audit in \path{runs/hpc/paper_matched_single_notch_v2/scientific_review/} reports full-overlap NRMSE \(0.245705\), pre-reference-peak RMSE \(0.044136\ \mathrm{kN}\), post-reference-peak RMSE \(0.348093\ \mathrm{kN}\), and overlap area-under-curve relative error \(0.193324\). Matched-force relative errors are \(27.48\%\) at \(U_2=0.002\ \mathrm{mm}\), \(4.98\%\) at \(U_2=0.005\ \mathrm{mm}\), and \(11.55\%\) at \(U_2\approx0.00599\ \mathrm{mm}\); the final \(U_2=0.0067\ \mathrm{mm}\) state lies outside the processed reference range.

At phase threshold \(\mathrm{SDV15}\ge0.95\), the final matched contour contains 193 element-mean damaged elements and an approximately horizontal connected crack extension of about \(0.0505\ \mathrm{mm}\). The forensic crack audit also records threshold and integration-point aggregation sensitivity. The ODB-wide bounds and irreversibility scan found no \texttt{SDV16} decreases. It found 6113 \texttt{SDV15} decreases and late-stage \texttt{SDV15} overshoot up to \(1.005600\). A detailed no-solution event reconstruction now preserves the raw 6113-event table, reproduces the historical count with unique keys, identifies 1297 events above the \(10^{-6}\) ODB precision tolerance, categorizes 480 as \texttt{staggered\_sync\_effect} and 817 as \texttt{insufficient\_mapping\_evidence}, and confirms 0 \texttt{SDV16} decreases at those checked locations. These diagnostics require scientific review before any validation claim.

The scientific classification is therefore:

\begin{center}
\texttt{paper\_matched\_v2\_scientific\_review\_incomplete}
\end{center}

The no-solution decision report is preserved at \path{runs/hpc/paper_matched_single_notch_v2/scientific_review/SCIENTIFIC_DECISION.md}. It decomposes the RF--U mismatch rather than relying on the single full-overlap NRMSE: pre-reference-peak RMSE is \(0.044136\ \mathrm{kN}\), post-reference-peak RMSE is \(0.348093\ \mathrm{kN}\), full-overlap NRMSE is \(0.245705\), tangent-stiffness error is about \(+51.4\%\), common-domain area error is \(+0.193324\), and maximum force error is \(0.691897\ \mathrm{kN}\). The dominant mismatch is post-peak.

The final crack path remains connected and approximately horizontal for thresholds \(0.80\), \(0.90\), \(0.95\), and \(0.99\), with no disconnected high-damage elements in the final matched contour. The final extension beyond the notch tip is threshold dependent: \(0.0555\ \mathrm{mm}\), \(0.0525\ \mathrm{mm}\), \(0.0505\ \mathrm{mm}\), and \(0.0465\ \mathrm{mm}\), respectively. Thus \(0.0505\ \mathrm{mm}\) is a high-damage-core measure, not a complete diffuse-crack length.

The detailed \texttt{SDV15} evidence is preserved under \path{runs/hpc/paper_matched_single_notch_v2/scientific_review/sdv15_detailed_review/}. The decision therefore no longer has a missing raw-table blocker, but it still does not classify all decreases as physical healing or dismiss all decreases as harmless because 817 above-precision non-staggered events lack equivalent phase-update-state proof in the retained ODB data.

Gate A3 remains \texttt{reference\_data\_insufficient}. No retry, second candidate, multi-CPU run, parameter study, MISESERI run, remeshing run, or state-transfer run is authorized by this completed one-run checkpoint.

\section{Remaining Gate A3 Work}

\begin{itemize}
  \item Obtain supervisor-approved tolerances for RF--U curve error, peak-force error, peak-displacement error, crack-path deviation, \texttt{SDV15} bounds/irreversibility interpretation, and runtime/cost metrics.
  \item Review whether the candidate-v2 RF--U deviations against the approximate Fig.~7 curve are acceptable for the thesis baseline.
  \item Resolve the 817 candidate-v2 \texttt{SDV15} above-precision non-staggered events categorized as \texttt{insufficient\_mapping\_evidence} before declaring irreversibility clean or failed.
  \item Decide whether the final crack extension and approximately horizontal crack path are sufficient for the paper-matched baseline.
  \item If candidate v2 is accepted, perform mesh-size, length-scale, and load-increment studies to justify the uniform fine reference.
  \item Do not start MISESERI, remeshing, state transfer, a retry, or a new Abaqus run until the candidate-v2 review route is accepted and explicitly authorized.
\end{itemize}

\section*{Reproducibility Files}

\begin{itemize}
  \item One-element scientific validator: \texttt{scripts/validation/check\_molnar\_one\_element.py}
  \item Single-notch extractor: \texttt{scripts/postprocessing/extract\_molnar\_single\_notch.py}
  \item Report figure generator: \texttt{scripts/postprocessing/generate\_stage\_a\_report\_figures.py}
  \item One-element evidence: \texttt{runs/molnar\_one\_element\_unchanged/20260714\_technical\_gate\_local/scientific\_check/}
  \item Single-notch evidence: \texttt{runs/molnar\_single\_notch\_unchanged/20260714\_technical\_gate\_local/}
  \item Paper-matched candidate-v2 evidence: \texttt{runs/hpc/paper\_matched\_single\_notch\_v2/}
  \item Report figures: \texttt{results/figures/stage\_a\_baseline/}
  \item Report tables: \texttt{results/tables/stage\_a\_baseline/}
\end{itemize}

\end{document}
