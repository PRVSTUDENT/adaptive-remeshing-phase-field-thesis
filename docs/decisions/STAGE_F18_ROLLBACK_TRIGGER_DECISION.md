# Stage F18 rollback-trigger decision

The forced lane requests `PNEWDT=0.5` once only at the qualified F17
penalty-active signature, using tolerances rather than exact floating equality.
The persistent latch is a job-directory flag file and is not stored in SVARS.
The control and forced lanes share byte-identical Fortran and input-deck bytes.
STA/MSG evidence is mandatory; UEL diagnostics alone cannot qualify rollback.
